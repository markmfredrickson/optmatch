# Displays a BlockedInfinitySparseMatrix

Displays each block of the BlockedInfinitySparseMatrix separately.

## Usage

``` r
# S4 method for class 'BlockedInfinitySparseMatrix'
show(object)
```

## Arguments

- object:

  An BlockedInfinitySparseMatrix to print.
