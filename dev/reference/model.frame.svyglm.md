# This method quells a warning when `optmatch::scores()` is applied to a svyglm object.

This method quells a warning when
[`optmatch::scores()`](https://markmfredrickson.github.io/optmatch/dev/reference/scores.md)
is applied to a svyglm object.

## Usage

``` r
# S3 method for class 'svyglm'
model.frame(formula, ...)
```
