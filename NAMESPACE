useDynLib(optmatch)

# export(fullmatch, pairmatch, makedist, pscore.dist, mahal.dist, matched.distances, matched, matchfailed, unmatched, maxControlsCap, minControlsCap, relaxinfo, stratumStructure, caliper, mdist, fill.NAs)
exportPattern("*")

S3method(print, optmatch)
S3method(summary, optmatch)
S3method("[", optmatch)
S3method(print, summary.optmatch)


